version 1: 1.8m for 10 billion rows
    cons:
        - lack of concurrency
        - terribly inefficient I/O 
        - loading everything into a single map (can load too much into ram which can cause a crash or force the system to start swapping)